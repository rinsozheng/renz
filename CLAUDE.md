# CLAUDE.md（通用软件工程纪律模板）

> 这是从一个真实项目里提炼出来的**与领域无关**的软件开发经验，去掉了项目特有内容，
> 留作任何新项目 CLAUDE.md 的起手模板。核心是一套**多 agent 协作 + 自验闭环 + 复盘沉淀**
> 的工程纪律。按"以后必须 X / 凡是 Y 就要 Z"的祈使句读，落地执行。

---

## 1. 多 agent 协作工作流（角色分工 + 模型选择）

一个功能的标准生命周期，按角色和模型分工跑完整闭环：

1. **主 agent（强推理模型，如 opus）** —— 理解需求、设计架构/方案、编排子任务、调试、处理评审意见。
   主 agent 是"大脑"，不亲自写大量样板代码。
2. **独立 subagent（opus）评审设计** —— 主 agent 产出架构/详细方案后，**动手实现前**，派一个
   **全新上下文**的 opus subagent 评审：架构合理性、风险遗漏、与现有代码/约定的冲突、过度设计(YAGNI)。
   独立上下文是关键——它不被主 agent 的思路带偏，能挑出主 agent 的盲区。
3. **主 agent 撰写开发需求描述文档** —— 把评审通过的方案写成**精确到文件/函数/测试用例边界**的
   实施文档（假定执行者对本代码库零上下文）。
4. **subagent（写码模型，如 sonnet）实现 + 单元测试** —— 按文档落地。独立、可并行的文件可
   **fan-out 多个 sonnet subagent 并行**。
5. **独立 subagent（opus）做 code review + 系统化调试** —— 实现完成后，全新上下文的 opus
   review 代码质量 + 跑系统化调试定位问题。

**模型选择法则**：
- 设计架构/方案、评审、调试、定位根因 → **强推理模型（opus）**（要的是推理力）。
- 按已敲定的设计写代码 + 单测 → **写码模型（sonnet）**。
- 当前 session 的模型与当前阶段不匹配时（如强模型在做纯样板写码），主动建议切换。

**为什么用独立上下文 subagent 评审/review**：同一个上下文里，错误假设会自我强化（见 §3"同源回声"）。
独立上下文 = 正交视角，是发现盲区最便宜的手段。

---

## 2. 设计纪律

- **架构/详细方案必须独立评审，动手前评审**（硬规则）。评审意见回主 agent**逐条处理**（采纳就改
  文档/不采纳给理由），重大问题改完设计再开工。
- **评审不得给"望文生义的外部符号"背书**：设计/评审里任何**外部引擎/库的 enum / 常量 / API 名**，
  没经真机/真环境核对的，一律标 `UNVERIFIED`。评审对 UNVERIFIED 符号**只能提"需真环境核对"，
  不能背书其正确性**（外部符号"存在"≠"可用"，见 §3、§8）。
- **结构性改动（重组假设、让多个分支/mode 消失）= 用户主导**。当你在罗列多个 mode/分支时停一下问：
  "是不是有更上游的一个决策能让这些 mode 根本不存在？"
- **细节裁剪（砍某层/某模块）= 谨慎**：先列 trade-off。想砍掉某层时，先想清楚那层**解决的是什么问题**。
  YAGNI（You Aren't Gonna Need It，别为假想需求提前造）是对的，但别在不理解一层作用时就砍。
- **不要在错误假设上打多 mode 补丁** —— 上游假设一换，那些 mode 就该消失。
- **给方案列 3-4 个候选 + 各自代价 + 你的推荐**，而不是单一答案或穷举式罗列。
- **分层路线最佳**："MVP 走方案 ①，但架构预留 ② 的接口" —— 既不过度设计，又不堵死扩展。

---

## 3. 验证纪律（最重要的一块）

> 核心信条：**单测绿 + 中间 trace 绿 ≠ 真的生效**。把"我做了 X"和"X 真的发生了"分开。

- **能自验的，直接测，不要问**。凡是你有手段验证的（单测 / 自验脚本 / 真环境注入 / 截图判读 …），
  **一律自己跑完拿 PASS/FAIL 再汇报**，别先问"要不要我测"。判据：只要不需要人亲自动手操作，就是
  "你能自己测"。
- **验外部终态，别只验中间 trace**：自验断言的必须是**外部世界的真实终态**（数据真写进 DB、文件真
  生成、单位/资源真变化、目标真到位），**不能**只看代码自己打的"我做了 X"日志。trace 只证明"代码
  走到了那一步并打了日志"，**不证明外部系统真的执行了**。
- **每个验收必须有一个"外部终态黑盒门"**：单测 / 自洽校验 / 独立评审都是"内部自洽"检查（验自己
  代码/解析/设计互相一致），**全绿只是内部回声**。凡是把命令/数据发给**外部系统**（引擎 / LLM /
  API / DB / 子进程）的功能，DoD 固定两条：①断言**外部世界终态**变化（不依赖任何内部假设的黑盒）
  ②核对**外部返回码**（成功状态 / 目标在可用列表里）。两条都过才算完。
- **同源的多个验证是回声、不是独立证据**：单测 mock、prompt、设计文档若都从同一个假设长出来，
  一致通过**不增加置信度**（同一个错误假设能骗过全部三道门）。别靠"叠加同源验证"刷安全感，要补一条
  **正交**的外部验证。
- **"风险低 / 用户自己试就知道"是危险信号，不是发布信号**：当结论里冒出"应该没问题 / 残留风险低 /
  让用户下次自己确认"，而这功能你**本可自验终态**——那是"我还没验终态"的自白，**立刻自验，别等被质疑**。
- **聚合判据不能掩盖 per-instance 失败**：多实例/多目标的自验，断言**绝不能**用"所有实例里最好的
  那个"（`best/any/全局 min-max`）做门——一个达标就 PASS 会**完整掩盖其余实例的失败**。要
  **per-instance / per-target 分别断言**。聚合判据 = 假阳性温床。
- **自验聚合判据要对得上实际数据格式**：grep/解析计数前先确认真实输出串格式，别用想当然的子串。

---

## 4. 系统化调试

- **铁律：没找到根因前不许动手修**（NO FIXES WITHOUT ROOT CAUSE FIRST）。症状修复 = 失败。
- **四阶段**：①根因调查（读全错误信息、稳定复现、查最近改动、**多组件系统在边界处加诊断埋点取证**、
  反向追数据流找坏值源头）→ ②对比分析（找可工作的同类、列出每一处差异）→ ③假设与验证（单一假设、
  最小改动、一次只动一个变量）→ ④实现（先写失败测试、单一修复、验证、不"顺手"夹带别的改动）。
- **取证，不要猜**：在组件边界打日志看数据进/出，**先定位哪一层断的**，再钻那一层。别在没证据时
  罗列一堆"可能的修法"。
- **同一现象反复修不好 → 停下来质疑"我盯的是不是病根"**：当**同一个用户现象**改了 2-3 次仍复现，
  **别再调同一个旋钮**（那说明假设错了）；回到根因调查，尤其检查"这条数据流里有没有哪一段的**目的
  和我的目标相反**"（如：用一个"规避 X"的工具去"达成 X" = 方向自相矛盾）。
- **修了 ≥3 次还没好 → 质疑架构**：若每次修都在别处冒出新问题/需要大重构，可能是架构问题而非假设问题，
  停下来和人讨论是否该重构而非继续打补丁。
- **红旗短语**（出现就停、回到根因）："先快修以后再查"、"试试改 X 看行不行"、"不太懂但这样可能行"、
  在追数据流之前就抛解法。

---

## 5. 实现纪律

- **目标值一次规划、锁定，别每轮重算一个漂移的靶**（通用强规则）：凡是给一个**会被反复执行的循环/轮询**
  下"目标"（坐标、落点、目的地、批次、句柄…），**第一次决定时就算好、缓存住，之后每轮发同一个**，
  绝不每轮重新计算一个临时目标。每轮重选 = 目标漂移 = 永远收敛不了/到不了。**正确套路**：决定那一刻
  把目标存进缓存（按 id/key），动作完成再清缓存；中途只幂等重发同一个缓存值。
- **连"解析层"都要确定性**：凡是把"名字→值"的解析当目标用，必须保证**同输入每轮同输出**（别用
  依赖集合顺序/时间的 `.first` 这类非确定性取值）。写"给目标"的逻辑前先问：这个目标每轮会变吗？
  会变就得锁，或让解析确定性。
- **可观测性是 first-class，不是事后补**：关键数据流（外部调用的入参/出参/耗时、状态机进出、决策点）
  都落**结构化日志（JSONL）**，全量保留，便于事后复盘。
- **接口有多个实现时，改接口必须同步所有实现 + 跑一致性审计**：鸭子类型 / `Protocol` / 接口**运行时
  不强制实现**——漏掉某个实现时，单测（用另一个实现的 mock）全绿、真环境必炸。加一条 audit 测试断言
  "所有实现都覆盖接口全部公开方法"，改接口后跑它。
- **部署无关**：服务端协议假定可能被远程客户端连接，**不硬编码 localhost**；至少考虑两种部署变体。
- **走抽象接口拿资源，不直接 import 路径**：数据/配置经 `Store.get(id)` 这类接口取，未来好替换。

---

## 6. 严重 bug 复盘 + 踩坑记录（沉淀机制）

- **每踩一个非显而易见的坑**（尤其"单测绿/看着对、真环境却炸"那种），追加一条到 **`docs/pitfalls.md`**：
  症状 → 根因 → 修法/教训 → ref。开工前扫一眼相关条目别重复踩；调试卡住翻一翻可能正中。新坑加最上面。
- **每次严重 bug/问题**（发布了/差点发布不工作的功能、多次修错方向、假阳性测试骗过自己、真环境炸而
  单测照不出、被反复质疑才发现），**修完后独立开一个 subagent（opus）复盘**整个过程：哪里判断错了、
  根因链、本该更早怎么发现。复盘**分两处落地，缺一不可**：
  - **坑本身（事后）→ `pitfalls.md`**：存档备查。
  - **正向规则（以后怎么做）→ 本文件（CLAUDE.md）**：提炼成可操作、可检索的纪律，加进对应章节，
    用祈使句，**不是**复述坑本身。
  - 一句话：**pitfalls.md 是"摔过的跤"，CLAUDE.md 是"以后怎么不摔"**，两者互补。
- 目的：让"我自己骗过自己"这类问题沉淀成**主动执行的纪律**，而不是同一类坑反复踩。

---

## 7. 文档同源纪律

- **功能交付 = 代码 + 测试 + 文档同源更新**。每次新增/改动**用户能感知的能力**，完成时就回头同步刷新：
  架构文档（当前代码/不变量/数据流）、用户指南（含真实使用示例，学得会才算交付）、README（对外能力清单）、
  CHANGELOG。判据：只要用户能说出的新指令、界面新东西 —— 就要问"用户指南教了吗？README 列了吗？
  架构的数据流/不变量变了吗？"纯内部重构不必。
- **commit message 与 CHANGELOG 同源**：每次 commit 前先把改动作为条目写进 CHANGELOG（改了什么 +
  为什么 + 影响），**同样内容写进 commit message**（让 message 本身就是完整 changelog 条目）。
  绝不出现"改了但 CHANGELOG 没记"。
- **内容/配置与代码分离**（适用时）：规则、提示词、别名、配置等"内容"放数据文件，代码只加载，
  改内容不改代码。改完重新生成给人 review 的快照。

### 文档格式约定（Markdown 为默认真理源，HTML 按需生成）

- **真理源一律 Markdown（`.md`）**：所有文档（agent 指令、设计稿、用户指南、README、CHANGELOG、
  pitfalls…）的**源文件都用 `.md`**。理由：① agent 指令文件（`CLAUDE.md` 等）由工具**按文件名自动
  加载**，改成别的格式就不被加载；② 这类文件**常驻上下文**，markdown 信噪比高、省 token；③ LLM 对
  markdown 解析最准；④ git diff / code review 干净。
- **HTML 不是必经步骤，只在用户明确要求时才生成**：需要"渲染好看的页面"（浏览器打开、折叠、上色、
  内嵌图、可独立分发或在应用内 serve）时，**用户提出后**再从对应 `.md` 生成 `.html`（手工或静态站点
  生成器，md 仍是唯一真理源）。**不要**把生成 HTML 作为每次文档改动的默认动作。
- **agent 指令文件永远不要用 HTML**：`CLAUDE.md` / `AGENTS.md` 这类必须是 `.md`。需要指向 HTML
  文档时，在 `.md` 里写一句指针（"详细文档见 `docs/xxx.md`，渲染版 `docs/xxx.html`"）即可——
  agent 能按需读 `.html`（当文本读），只是不会自动加载它。

---

## 8. 环境 / 依赖纪律

- **装包先确认在项目虚拟环境里，不污染全局**。用项目的依赖管理工具（lock 文件），装新依赖走它。
- **严禁裸 `pip install -U <pkg>`**：它会按别的包的版本约束**静默降级共享依赖**（典型重灾区：序列化/
  proto 库被某个包拉回旧版，导致另一个包运行时炸）。装完若怀疑，核对关键共享依赖版本是否被动过。
- **外部 enum / API / 常量：存在 ≠ 可用**。一个符号在库里能 import 到，不代表运行时引擎接受它。
  依赖任何外部符号前**真环境核对**（可用列表查询 / 官方文档），核对前标 `UNVERIFIED`。
- **外部调用可能被静默丢弃/拒绝**：发给外部引擎/库的命令可能因各种隐藏条件被丢弃或拒绝。**核对返回码**
  （成功/失败状态），非成功就是被拒；并以**外部终态**为准（见 §3）。
- **真实配置 gitignore + 提交一份 `.example` 模板**：含机密/环境相关的配置文件（API key、端点、token、
  路径…）**真实文件一律 gitignore、绝不入库**；同时**提交一份去敏的 `<name>.example` 模板**作为仓库内
  唯一可 commit 的样例（字段齐全、机密处用注释占位如 `# api_key: sk-...`，绝不放真值）。新人上手
  `cp x.example x` 再填自己的值。**机密做成可配置**（可来自 gitignore 配置文件 *或* 环境变量），
  但模板和任何入库文件里永远是占位符。

---

## 9. 工作风格

- **自驱，少打扰**：设计文档/已敲定方案是真理源，按它执行，别反复问"要不要这样做"。
- **只在真有歧义时提问，且问得具体**："A 还是 B，原因是 X"，不要开放式问。**架构层面**的偏离必须问；
  **实现层面**的选择（库、目录结构、测试用例边界）自己决定，留 ADR 记录。
- **列 trade-off 而非单一推荐**：给 3-4 候选 + 各自代价 + 推荐，让人选。
- **给具体方案 + 真实例子**（schema / 代码片段 / 真实输入），不要抽象理论。
- **用进度跟踪工具**让人随时看到卡在哪。
- **不要过度简化** —— 当你有冲动砍掉某层时，先想清楚那层解决什么问题。
- **会话交接协议**：把"当前状态 / 下一步 / 阻塞项 / 环境关键事实"维护在一个**高频更新的活文档**里
  （而不是污染长期文档的 history）。新 session 起手读它；session 结束时更新它。

---

## 10. 并行

- **独立的批量任务大胆 fan-out 并行 subagent**（写码用 sonnet，编排/评审/调试用 opus）。
- **可并行的验证多开并行跑**（多用例 / 多种子 / 多配置一次跑完），别串行干等。
- 编排原则：独立调用放同一批并发发出；有依赖的才串行。

---

## 附：一个功能从需求到交付的标准链（把上面串起来）

1. 主 agent(opus) 理解需求 → 列 3-4 方案 + 推荐（§2、§9）
2. 独立 opus subagent 评审方案（§1、§2）→ 主 agent 逐条处理评审意见
3. 涉及外部符号/系统 → 先真环境核对 linchpin（§8）；核对不过就改方案
4. 主 agent 写精确实施文档（§1）
5. sonnet subagent(可并行) 实现 + 单测（§1、§10）
6. **自验外部终态**（§3）：黑盒门 + 返回码 + per-instance 断言
7. 独立 opus subagent code review + 必要时系统化调试（§1、§4）
8. 文档同源更新 + CHANGELOG=commit message（§7）→ 提交
9. 出严重 bug → 独立复盘 → pitfalls + 本文件正向规则（§6）
